# In-process Embedding-Backend: fastembed plus transitiv onnxruntime/tokenizers und
# der vorgebackene Modell-Cache — zusammen rund 250 MB Image (413 statt 162 MB).
# Nur im Default-Image installiert; das :slim-Image laesst beides weg und braucht
# dafuer EMBED_URL auf einen externen OpenAI-kompatiblen /v1/embeddings-Endpoint.
# Pinning-Regeln wie in requirements.txt (Issue #27, Dependabot-gated).
fastembed==0.8.0

# Exakt gepinnt (Issue #27, Tier 2): reproduzierbarer Build, kein stilles Brechen
# durch Upstream-Bumps. Versionen = pip freeze aus dem laufenden, known-good Image.
# Kontrollierte Bumps laufen über Dependabot-PRs (CI-gated).
# ponytail: nur Top-Level gepinnt — Transitive floaten noch; echtes Lockfile
# (uv/pip-compile) erst nachrüsten, wenn ein Transitiv-Bump real beißt.
fastmcp==3.4.7
kuzu==0.11.3
numpy==2.5.2
cryptography==50.0.0
pyyaml==6.0.3

# fastembed (in-process Embeddings) steht in requirements-embed.txt und wird nur
# ins Default-Image installiert — das :slim-Image faehrt ueber EMBED_URL extern.
